#include <stdio.h>
#include <math.h>
#include <string.h>
#include <unistd.h>

const float theta_spacing = 0.07; //when to calculate next value of X,Y,Z based on theta 
const float phi_spacing = 0.02; //when to calculate next value of X,Y,Z based on phi

// constants from the solid of revolution concept of creating the torus
const float R1 = 1;
const float R2 = 5;

const float K2 = 5; //3D modelling on 2D constant 
const float K1;
int main()
{
  printf(screen_width);
  return 0;
}
